# ガイドの統合確認

2026-09-08。`xrift-studio-wiki-rebuilt.zip` を、コミット `b3c5388` の編集UIへ統合した。

## 取り込み時の変更

- Assetsの密度、フォルダーと種類の区別、配置の右クリック操作、Hierarchyの作成メニューを保持した。
- 旧Wikiの21原稿、表示コード、旧URL互換処理、移行ページ、移行マップを削除した。旧原稿の別フォルダーへのコピーも残していない。
- 新しい30原稿を公開版とアプリ内ヘルプで共有する。
- 原稿の操作名を現在のSphere、Transform、Mesh Renderer、Add Componentに合わせた。
- Node環境のJSON読み込みと、旧文言に依存するテストを更新した。

## 確認した範囲

- `pnpm typecheck`、`pnpm e2e:typecheck`：成功。
- ガイド構造テスト：10件成功。
- 既存の日本語表記・UIフローテスト：58件成功。
- ガイドとコンポーネントメニューのPlaywrightテスト：13件成功。30記事を320・390・768・1440pxで確認し、横はみ出しなし。検索、見出し移動、検索失敗、JavaScriptなしの本文を確認した。
- Markdown変換はインストール済みのReactMarkdownを使用し、Vite開発サーバーへの通常のURL移動で確認した。変換の代替実装は使っていない。

## 未実施

本番ビルド、公開・デプロイ、Tauri実機の保存と公開、初見利用者による操作確認は実施していない。参考画像はZIPのものを保持しており、現行実機の操作画像への差し替えは公開前の確認事項として残る。

添付レビュー内の旧URL対応や専門原稿の移管に関する記述は、ZIP作成時点の記録であり、統合後の構成には適用しない。

追加のPlaywright確認5件も成功した。旧ページ・移行マップの404、アプリ内ヘルプの読込とフォーカス復帰、320pxの再確認、初回セットアップ成功・失敗時の導線を確認した。
