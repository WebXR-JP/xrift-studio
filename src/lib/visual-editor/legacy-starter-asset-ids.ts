/**
 * Asset ids minted by starter templates that no longer exist.
 *
 * The Studio guide and OpenBrush world templates were removed from the
 * registry, but projects created while they shipped still carry the assets they
 * seeded. The editor keeps recognising those ids so an existing project keeps
 * rendering as its author left it; nothing creates them any more.
 */
export const STUDIO_GUIDE_INTERACTION_DOOR_MODEL_ASSET_ID =
  "starter-model-studio-guide-interaction-door";
