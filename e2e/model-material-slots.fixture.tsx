// Use Vite's resolved React instance for the isolated browser fixture.
import React from "react";
export { React };
export { createRoot } from "react-dom/client";
export { ModelMaterialSlots } from "../src/components/visual-editor/ModelMaterialSlots";
